@{
    Layout = "_Layout";

}

@using BestRestaurants.Models;


<h1>Cuisine</h1>

@if (@Model.Count == 0)
{
    <h3>No cuisine has been added!</h1>
}

@foreach (Cuisine cuisine in Model)
{
    <li>@Html.ActionLink($"{cuisine.}")
}